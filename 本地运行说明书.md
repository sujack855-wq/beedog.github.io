# BeeDog 个人网站 · 本地运行说明书

> 给自己看的大白话版。按顺序照着做就行。

---

## 📦 这个网站是啥

一个纯静态网站,只用了 HTML + CSS + JS,**没有任何框架、不需要安装任何东西**。
- 不用装 Node.js
- 不用 npm install
- 不用 build / 打包
- 双击 `index.html` 理论上就能看,但**推荐用本地服务器**(否则有些功能比如字体和 JS 脚本可能会被浏览器拦掉)

---

## 🖥 方法一:最省事 — VS Code + Live Server(推荐)

### 步骤
1. 装 **VS Code**(如果还没装):https://code.visualstudio.com/
2. 打开 VS Code,左边扩展商店(Ctrl/Cmd + Shift + X)里搜 **Live Server**,点安装
3. 把本项目文件夹拖进 VS Code 窗口
4. 左侧文件树找到 `index.html`,**右键** → **Open with Live Server**
5. 浏览器会自动打开 `http://127.0.0.1:5500/index.html`,网站就跑起来了
6. 改任何文件按 Ctrl/Cmd + S 保存,浏览器会**自动刷新**,不用自己按 F5

**适合谁:** 以后还想改内容/文案的人。这是最舒服的方式。

---

## 🐍 方法二:Python 一行命令(不想装插件)

电脑上大概率已经有 Python 了。

### 步骤
1. 打开终端(Mac:Terminal / Windows:PowerShell 或 CMD)
2. `cd` 进到项目文件夹,比如:
   ```
   cd ~/Downloads/个人网站/site
   ```
   (Windows 里路径用反斜杠或者直接把文件夹拖到终端窗口里)
3. 敲一行命令:
   ```
   python3 -m http.server 8000
   ```
   Windows 如果报错就用 `python -m http.server 8000`
4. 浏览器打开 **http://localhost:8000**
5. 关闭就按 Ctrl + C

---

## 🟢 方法三:Node 用户(如果你正好装了)

```
npx serve
```
或者
```
npx http-server -p 8000
```
打开提示里的地址就行。

---

## 📁 直接双击打开 HTML 可以吗?

**可以,但不完全能用。**
- 双击后地址栏是 `file:///...`,这种模式下浏览器会拦掉 localStorage 之外的一部分脚本
- SCORE 累积、留言板、小游戏大部分还能用
- 但可能样式/字体加载异常

**结论:能用方法一/二就别双击。**

---

## 🌍 想发到网上给别人看?(GitHub Pages)

免费方案,5 分钟搞定:

### 步骤
1. 去 GitHub 新建一个仓库,名字随便,比如 `beedog-site`
2. 本地进项目文件夹,敲:
   ```
   git init
   git add .
   git commit -m "init"
   git branch -M main
   git remote add origin https://github.com/你的用户名/beedog-site.git
   git push -u origin main
   ```
3. 到这个仓库的 **Settings → Pages**
4. Source 选 `Deploy from a branch`,Branch 选 `main` / `(root)`,Save
5. 等 1-2 分钟,页面会显示一个网址,比如 `https://你的用户名.github.io/beedog-site/`
6. 打开就能看到了

以后想更新内容:改完文件 → `git add . && git commit -m "更新" && git push`,网站自己会更新。

---

## 🗂 文件结构速查

```
site/
├─ index.html              首页(有像素背景)
├─ about.html              关于我
├─ portfolio.html          作品集
├─ blog.html               博客列表
├─ blog-detail-*.html      博客详情
├─ books.html              📚 道具栏书架
├─ book-note-*.html        具体书评
├─ travel.html             🗺 像素世界地图
├─ now.html                ● 当下 NOW
├─ game.html               🎮 蜜蜂小游戏
├─ guestbook.html          💾 访客留言板
├─ contact.html            联系方式
├─ css/
│  ├─ style.css            主样式(像素游戏风基础)
│  ├─ effects.css          动效/彩蛋
│  ├─ pixel-extras.css     ★ 新页面专用样式
│  ├─ about.css            about 页专属
│  ├─ blog.css             blog 页专属
│  ├─ contact.css          contact 页专属
│  └─ portfolio.css        portfolio 页专属
├─ js/
│  ├─ main.js              导航/SCORE/光标/Konami/成就
│  └─ effects.js           鼠标粒子/按钮星爆
└─ images/                 图片资源
```

---

## ✏️ 常见想改的地方

| 想改什么 | 打开哪个文件 | 找什么 |
|---|---|---|
| 书单 | `books.html` | 搜 `const BOOKS = [` |
| 去过的城市 | `travel.html` | 搜 `map-pin` |
| NOW 页面文案 | `now.html` | 直接改正文 |
| 导航栏链接 | 每个页面都有 | 搜 `<ul class="nav-menu"` |
| 主色调(蜜蜂黄) | `css/style.css` | 搜 `--bee-yellow` |
| 首页 Hero 标题 | `index.html` | 搜 `hero-title` |

---

## ⚠️ 几个小提醒

1. **留言板是本地存的** — 每个访客只能看到自己写的。想做真·公开留言板需要后端(超出纯静态网站范围)
2. **字体用了 Google Fonts** — 没网时字体会退回系统字体,但不影响功能
3. **视频文件** — 我改成了纯 CSS 像素背景,不再需要 `videos/` 文件夹,可以删掉
4. **SCORE 分数** — 存在浏览器 localStorage,换浏览器会清零,这是正常的

---

## 🆘 卡住了?

- 页面白屏 → F12 打开控制台看红色报错,大概率是文件没在同一目录
- 样式全没了 → 确认你是用"服务器"打开而不是双击 file://
- Live Server 没反应 → VS Code 重启一下
- git push 要登录 → 用 GitHub 的 Personal Access Token 当密码

祝你折腾愉快 🐝
